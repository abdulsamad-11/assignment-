    //circumference of a circle
    
    const calcCircumference = function () {
        radius = Math.radius;
            pi = Math.PI;
        return (2 * pi * radius);
    }
    calcCircumference.radius(5);   
    calcCircumference();
    console.log("the circumference is = " + calcCircumference());


    //area of a circle

    const calcArea = function() {   
        radius = Math.radius;
            pi = Math.PI; 
        return (pi * radius * radius);
    }
    calcArea.radius(5);
    calcArea();
    console.log("The Area is = " + calcArea());







