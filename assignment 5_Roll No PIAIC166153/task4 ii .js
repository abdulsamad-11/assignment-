

    //area of a circle

    function calcArea() { 
        var radius = Number(prompt("enter a radius"));
        pi = 3.14; 
        calcArea = pi * radius * radius;
        console.log("The Area is = " + calcArea);
    }
    calcArea();
    
